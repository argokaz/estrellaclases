name: Teacher auth migration (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Migration status
        run: |
          echo "Automatic migration is disabled."
          echo "The validated implementation lives on security/harden-teacher-auth-run."
